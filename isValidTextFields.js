

export default function isValidTextFields(field1, field2, field3, field4, ... fieldN) { 
    if (text1 === "" || text2 === "") {
      return alert("field is empty");
    }
    return true;
  };


  //1. add if
  //2. non limit args


isValidTextFields("", "text", 12, "s");
